tupla = tuple(["dato1", "dato2"]) #Funcion que permite crear una tupla ( tuple() )

#Creando una tupla sin parentesis de multiples datos
tupla = "dato1", "dato2" 

#Creando una tupla sin parentesis de un solo dato
tupla = "dato",

#Las tuplas sirve crearlas cuando necesitamos datos de solo lectura, cuando vamos a leer datos. Se alamcena en un lugar distinto a las listas. 

